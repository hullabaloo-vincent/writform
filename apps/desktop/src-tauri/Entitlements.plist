<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Tauri signs with the hardened runtime; without these entitlements
	     macOS refuses mic/camera BEFORE the permission prompt can appear
	     (Info.plist usage strings and TCC state are irrelevant at that
	     point). This was why the built app never asked. -->
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<key>com.apple.security.device.camera</key>
	<true/>
</dict>
</plist>
